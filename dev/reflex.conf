# Reflex configuration for hot reloading
# Watch for changes in .go files and rebuild/restart the server

-r '\.go$' -s -- sh -c 'go build -o /tmp/conjurctl ./cmd/conjurctl && /tmp/conjurctl server --bind-address 0.0.0.0'
